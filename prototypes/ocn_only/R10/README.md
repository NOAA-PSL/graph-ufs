# Ocn-only R10
This is 24h, 1IC, fineTop configuration.
In this run, we train a 24 hour ocean-only emulator using the same hyperparam 
configuration as R5 but with the fineTop setting.
Note that, here the training would be using MSE only, not using the Mahalanobis
loss as done in R8. 

# Configurations:
[T1M] The first full-blown training.  
       
